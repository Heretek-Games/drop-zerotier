{
  "name": "@drop/zerotier-addon-client",
  "version": "0.3.0",
  "private": true,
  "description": "Drop client addon: joins ZeroTier mesh networks for multiplayer sessions",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/test/*.js"
  },
  "dependencies": {
    "@droposs/plugin-sdk": "^0.6.1"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^5.7.2"
  }
}
