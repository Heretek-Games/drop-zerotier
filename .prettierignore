# Generated by the drop-plugin signer/packer (`JSON.stringify` output, not
# Prettier-formatted) and rebuilt by `npm run bundle:build` / `sign` / `pack`.
plugin-bundle/drop-plugin.json
plugin-bundle/dist/
plugin-bundle/dist-package/
dist-packages/
